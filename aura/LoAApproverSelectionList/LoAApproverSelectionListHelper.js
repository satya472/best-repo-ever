/**
 * Created by jens.becker on 10.04.18.
 */
({


    /*
    preselect: function(cmp, event){
        var component = cmp.find("ApproverList");
        var rows = ['a1M9E000000YCFUUA4'];

        cmp.set('v.selectedRowsNew',rows);
    }
    */
})